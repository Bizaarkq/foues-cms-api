{
  "collectionName": "components_blocks_key_dates",
  "info": {
    "displayName": "Calendario",
    "icon": "calendar",
    "description": "Fechas importantes en vista de calendario (un mes) o lista cronológica, con categoría opcional por fecha."
  },
  "options": {},
  "attributes": {
    "title": {
      "type": "string"
    },
    "display_mode": {
      "type": "enumeration",
      "enum": [
        "calendar",
        "list"
      ],
      "default": "list"
    },
    "items": {
      "type": "component",
      "repeatable": true,
      "component": "elements.date-entry"
    }
  }
}
