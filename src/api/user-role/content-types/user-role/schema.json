{
  "kind": "collectionType",
  "collectionName": "user_roles",
  "info": {
    "singularName": "user-role",
    "pluralName": "user-roles",
    "displayName": "Rol de usuario",
    "description": "Roles de los usuarios del sitio (Estudiante, Catedrático, Personal…). El rol con 'is_default' se asigna automáticamente a los usuarios nuevos."
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "name": {
      "type": "string",
      "required": true
    },
    "key": {
      "type": "uid",
      "targetField": "name"
    },
    "description": {
      "type": "string"
    },
    "is_default": {
      "type": "boolean",
      "default": false
    }
  }
}
